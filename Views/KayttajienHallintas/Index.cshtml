@model IEnumerable<TilausJärjestelmä.Models.LoginsVM>

@{
    ViewBag.Title = "Index";
}



<div class="d-flex">
    <h2>Käyttäjätietojen hallinta</h2>
    <h3 style="margin-left: 50%">
        <a class="btn btn-secondary col-12" @Html.ActionLink("Uusi Käyttäjä", "Create")
    </h3>

</div>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayName("Turvallisuus taso")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                @Html.ActionLink("Muuta oikeuksia", "Edit", new { id = item.LoginId }) |

                @Html.ActionLink("Poista käyttäjä", "Delete", new { id = item.LoginId })
            </td>
        </tr>
    }

</table>
